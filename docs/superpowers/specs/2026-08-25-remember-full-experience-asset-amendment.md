# REMEMBER Full Experience — Asset Amendment

This amendment supersedes the **Known Asset Gap** and conditional residual-scar fallback in `2026-08-25-remember-full-experience-design.md`.

## Confirmed asset

`/remember-experience/assets/images/mr06-restored-scar-overlay.png`

The asset is now present in `main` and has been synchronized into `feat/remember-full-experience`.

## Updated restoration behavior

`mr06-restored-scar-overlay.png` is the canonical residual-scar layer used after the restored image consolidates.

- It appears after the Kintsugi propagation / memory pulse and restored-image consolidation.
- It remains visible at restrained opacity for roughly 700–1200ms during the completion microclimax.
- It then recedes to a subtle stable residue or near-zero opacity according to the memory-specific visual preset.
- Per-memory SVG crack paths remain authoritative for propagation geometry; `mr06` is the final visual residue, not a replacement for those paths.
- Reduced-motion uses a short opacity transition rather than animated propagation.

There is no longer an asset fallback required for the residual scar in the approved implementation.
